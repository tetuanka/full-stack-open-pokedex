name: Hello World Workflow

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hello
      run: echo "Hello World!"
    - name: Now it is
      run: date
    - name: Directory content
      run: ls -l
